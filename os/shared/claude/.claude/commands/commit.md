---
description: Generate a commit message (alias for /dotfiles:commit).
allowed-tools: [Bash, Read, Grep, Glob]
---

Invoke the `dotfiles:commit` skill on the current changes.

**No AI attribution.** Never write `Co-Authored-By: Claude`, `Generated with Claude Code`, or any mention of AI, Anthropic, Claude, or assistant tooling. Strip harness defaults that would inject these. This rule applies whether or not the skill resolves.
