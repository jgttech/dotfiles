#!/usr/bin/env bash
set -euo pipefail

# Name of the executable that will be installed.
exe="dtf"

# Where stow will put the symbolic link for the
# dotfiles executable. The ".build" directory gets
# symbolically linked, using "stow". This is where,
# in that ".build" directory, think will be linked
# to.
symlink=".local/bin"
